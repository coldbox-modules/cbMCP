/**
 * Copyright Since 2005 ColdBox Framework by Luis Majano and Ortus Solutions, Corp
 * www.ortussolutions.com
 * ---
 */
class {

	// Module Properties
	this.title 				= "cbAITools"
	this.author 			= "Ortus Solutions"
	this.webURL 			= "https://www.ortussolutions.com"
	this.description 		= "A collection of AI powered tools for ColdBox."
	this.version 			= "@build.version@+@build.number@"

	// Model Namespace
	this.modelNamespace		= "cbAITools"

	// Engine Mapping
	this.mapping			= "cbAITools"

	// Dependencies
	this.dependencies 		= []

	// URI Endpoint Prefix
	this.entryPoint		= "cbai"

	/**
	 * Configure Module
	 */
	function configure(){
		settings = {
		}
	}

	/**
	 * Fired when the module is registered and activated.
	 */
	function onLoad(){
		// Register our MCP Server here
		mcpServer(
			name: "cbAITools",
			description: "A collection of AI powered tools for ColdBox.",
			version: "@build.version@+@build.number@",
			cors: "*",
			statsEnabled: true,
			force: true
		)
		// Register tools
		.scan( "#modulePath#/models/tools" )
	}

	/**
	 * Fired when the module is unregistered and unloaded
	 */
	function onUnload(){
		aiService().removeServer( "cbAITools" )
	}

}
